[pytest]
asyncio_mode = auto
testpaths = tests

# Markers
markers =
    slow: latency benchmark tests / loads ML models — run with: pytest -m slow --live
    live: requires real Anthropic API key and running backend

# Default: exclude slow benchmarks from the regular CI run
addopts = -m "not slow"
