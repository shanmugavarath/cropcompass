[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests

# Markers
markers =
    slow: latency benchmark tests — run with: pytest -m slow --live
    live: requires real Anthropic API key and running backend

# Default: exclude slow benchmarks from the regular CI run
addopts = -m "not slow"
