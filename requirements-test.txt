# CropCompass — test dependencies (Member 5 — Task 5.5)
# Install: pip install -r requirements-test.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # async HTTP client + ASGI test transport
anthropic>=0.30      # patched in CI; real calls in --live mode
